# Raw data - intentionally never committed (fetched fresh, then discarded, on every run)
Datasets/Weather_Pollution_Data.csv

# NOTE: Datasets/Forecasting_Data.csv is NOT ignored - it IS committed,
# because AQI_Forecast_App.py reads it directly at runtime.

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store

# Temporary model experiments
Temporary Test Models/